title: Anypoint Platform Patterns
baseurl: "/anypoint-platform-patterns" # https://github.com/jekyll/jekyll/issues/332#issuecomment-18952908
author: Gerald Loeffler
email: gerald@integrational.eu
github_username: integrational
rss: rss
markdown: GFM
theme: minima
plugins:
  - jekyll-sitemap
  - jekyll-feed
collections:
  patterns:
    output: true
titles_from_headings:
  strip_title: true
  collections: true
minima:
  date_format: "%Y-%m-%d %H:%M"
feed:
  tags: true
defaults:
  - scope:
      type: patterns
    values:
      layout: page
header_pages:
  - patterns.md
# keywords:
# for 'about' in patterns use: "arch" (= architecture), "dev" (= development), "ops" (= operation) 
AP: "[Anypoint Platform](https://www.mulesoft.com/platform)"
